# modularized with gnu stow
# . $HOME/.bashrc_pop
if [ -f $HOME/.bashrc_jot ]; then
  . $HOME/.bashrc_jot
fi
if [ -f $HOME/.bashrc_branch_prompt ]; then
  . $HOME/.bashrc_branch_prompt
fi
if [ -f $HOME/.bashrc_go ]; then
  . $HOME/.bashrc_go
fi
if [ -f $HOME/.bashrc_rust ]; then
  . $HOME/.bashrc_rust
fi
